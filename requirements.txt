# 项目依赖版本锁定文件
# 锁定具体版本号以确保构建可重现
# 
# 使用说明：
# - 运行 `pip install -r requirements.txt` 安装所有依赖
# - 版本号格式：主版本.次版本.修订版本
# - 使用精确版本（==）而非范围（>=）确保环境一致性
# - 如需更新，请先在测试环境验证兼容性

# HTTP请求库 - 用于GitHub API和网站可访问性检测
requests==2.32.5